node_modules/
dist/
.next/
.env
.env.local
.env*.local
*.log
.superpowers/
apps/api/uploads/
test-results/
playwright-report/

# Load-test fixtures: tokens.json holds live candidate invite tokens for a seeded
# exam. Regenerate with `node load/seed-load-fixtures.js N` -- never commit it.
load/tokens.json

# Claude Code local session/config + skills lockfile (per-machine tooling state, not product)
.claude/
skills-lock.json
